---
import Layout from "../../layouts/Layout.astro"; 
import CardReal from "../../components/CardReal.astro";
import { getReals, getReal } from "../../../backend/backend.mjs";

const {id} = Astro.params;

let message ='';
let reals = [];

reals = await getReals() || [];

if (!reals){
	return Astro.redirect ('/reals');
}
console.log(reals)
---

<Layout titre=realisateurs>
    <h1 class="my-12 text-center">Liste des réalisateurs</h1>
    <div class="">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-10">
            {reals.map((real) => <CardReal {...real} />)}
        </div>
    </div>

</Layout>