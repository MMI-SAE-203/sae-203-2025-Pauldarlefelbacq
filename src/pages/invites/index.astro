---
import Layout from "../../layouts/Layout.astro"; 
import CardInvites from "../../components/CardInvites.astro";
import { getInv, getInvs } from "../../../backend/backend.mjs";

const {id} = Astro.params;

let message ='';
let invs = [];

invs = await getInvs() || [];

if (!invs){
	return Astro.redirect ('/invs');
}
console.log(invs)
---

<Layout titre=invites>
    <h1 class="text-center my-12">Liste des invités</h1>
    <div class="">
        <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-3 gap-4 mt-12">
            {invs.map((inv) => <CardInvites {...inv} />)}
        </div>
    </div>

</Layout>